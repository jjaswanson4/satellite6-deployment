---

- name: install satellite
  hosts:
    - satellite
  pre_tasks:
    - name: load in satellite vars
      include_vars:
        file: "{{ satellite_vars_file }}"
  roles:
    - install_satellite
    - tune_satellite
